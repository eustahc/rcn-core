{
  "name": "root",
  "private": true,
  "scripts": {
    "package:create": "npx lerna create",
    "package:build": "npx lerna run build",
    "package:push": "npx lerna publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "root": "file:"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
