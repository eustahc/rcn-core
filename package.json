{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "push": "npx lerna publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "root": "file:"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
